# MIRROR//AI Architecture

The deterministic Game Engine is the source of truth for stats, XP, time,
consequences, progression, timeline, and validation.

### Ownership
- Shivank: deterministic engine + backend/API + database + application integration
- Shivani: Grok AI + profile + scenarios + choices + memory
- Suraj: Future Self + counterfactual + Babylon.js
- Shiva: microphone + STT + TTS + voice UX

Never duplicate an existing contract. Inspect the repository before adding files.
